import math
import random as rd #вместо random будет rd
import moda
import modb
from modc import * # это значит все можно напрямую использовать
                   # также можно указать конкретную переменную или функцию вместо *

print(b)

print(modb.foo)
moda.bar()
print(math.pi)

print(math.sin(30))

print(rd.randint(1, 10)) # случаное число от 1 до 10